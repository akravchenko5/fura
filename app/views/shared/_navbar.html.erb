<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to root_path, class: "mr-2" do %>
      <%= image_tag("icons/logo_white", height: '35') %>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-expanded="false">
            Dropdown
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><a class="dropdown-item" href="#">Action</a></li>
            <li><a class="dropdown-item" href="#">Another action</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
      </ul>
      <div class="d-flex align-items-center">
        <span class="text-light mr-2"><%= user_signed_in? ? current_user.email : "Guest" %></span>
        <div class="nav-item dropdown">
          <div  class="avatar" 
              id="navbarDropdown" 
              data-toggle="dropdown" 
              aria-haspopup="true" 
              aria-expanded="false" >
            <%= avatar_helper %>
          </div>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <%= link_to icon('fa', 'cog', class: 'mr-2') + "Account", edit_user_registration_path, class: "dropdown-item" %>
              <%= link_to icon('fa', 'sign-out-alt', class: 'mr-2') + "Logout", destroy_user_session_path, class: "dropdown-item", method: :delete %>
            <% else %>
              <%= link_to icon('fa', 'sign-in-alt', class: 'mr-2') + "Login", new_user_session_path, class: "dropdown-item" %>
              <%= link_to icon('fa', 'user-plus', class: 'mr-1 text-info') + "Sign Up", new_user_registration_path, class: "dropdown-item text-info" %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
