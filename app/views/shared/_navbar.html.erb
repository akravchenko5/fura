<nav class="navbar navbar-expand-sm navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to root_path, class: "me-2" do %>
      <%= inline_svg_tag("icons/logo_white.svg", options={height: "2.2em"}) %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Cargos', cargos_path,  class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Dropdown
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><a class="dropdown-item" href="#">Action</a></li>
            <li><a class="dropdown-item" href="#">Another action</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
      </ul>
      <div class="navbar-nav">
        <span class="text-light me-2"><%= user_signed_in? ? current_user.email : "Guest" %></span>
        <div class="nav-item dropdown">
          <div  class="avatar"
              role="button" 
              id="navbarDropdown" 
              data-bs-toggle="dropdown" 
              aria-expanded="false" >
            <%= avatar_helper %>
          </div>
          <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <% if user_signed_in? %>
              <%= link_to (fa_icon 'cog', text: "Profile"), edit_user_registration_path, class: "dropdown-item" %>
              <%= link_to (fa_icon 'sign-out', text: "Logout"), destroy_user_session_path, class: "dropdown-item", method: :delete %>
            <% else %>
              <%= link_to (fa_icon 'sign-in', text: "Login"), new_user_session_path, class: "dropdown-item" %>
              <%= link_to (fa_icon 'user-plus', class: 'text-info', text: "Sign Up"), new_user_registration_path, class: "dropdown-item text-info" %>
            <% end %>
          </ul>
        </div>
      </li>
    </div>
  </div>
</nav>
